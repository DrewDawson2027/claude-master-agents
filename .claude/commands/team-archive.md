# /team-archive

Archive a team into a compressed snapshot and remove active references.

Primary tool:
- `coord_team_archive team_id="my-team" force_stop=true kill_panes=true`

Safe option:
- `keep_team_dir=true` to archive without deleting the team directory
