# /team-dashboard

Show a one-view dashboard for members, tasks, recent events, and today's team cost (if available).

Primary tool:
- `coord_team_dashboard team_id="my-team"`

Useful follow-ups:
- `coord_team_doctor team_id="my-team"`
- `coord_cost_team team_id="my-team" window="today" breakdown=true`
