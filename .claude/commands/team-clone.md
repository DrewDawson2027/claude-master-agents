# /team-clone

Clone a team structure for a new repo/workstream with reset runtime state.

Primary tool:
- `coord_team_clone team_id="my-team" new_team_id="my-team-v2" new_name="My Team V2" cwd="/path/to/repo"`

Options:
- `without_tasks=true`
- `copy_task_status=true`
