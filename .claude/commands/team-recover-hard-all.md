# /team-recover-hard-all

Run `recover-hard` across all active teams and write a sweep report.

Primary tool:
- `coord_team_recover_hard_all ensure_tmux=true snapshot_window="today" cost_timeout=20`

Output:
- report path under `~/.claude/reports/team-recover-hard-all-*.md`
