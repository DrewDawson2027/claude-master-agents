# /team-teardown

Stop a team and write a teardown summary snapshot (members, tasks, recent events, cost if available).

Primary tool:
- `coord_team_teardown team_id="my-team" kill_panes=true`

Alternative (leave tmux session running):
- `coord_team_teardown team_id="my-team" kill_panes=false`
