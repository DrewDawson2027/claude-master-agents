# /team-checkpoint

Create a parity-gated checkpoint archive for a team (Phase E starter).

Use `coord_team_checkpoint` with:
- `team_id`
- optional `label`
- optional `force` (bypass parity gate)
- optional `include_shadow=false`
