# /team-restart-member

Restart a teammate while preserving member ID, role, and claimed task ownership.

Primary tool:
- `coord_team_restart_member team_id="my-team" member_id="coder-1"`

Optional overrides:
- `cwd`, `agent`, `model`, `initial_prompt`
