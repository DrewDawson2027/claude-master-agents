# /team-replace-member

Replace a failed teammate with a new member ID and transfer task/worker ownership.

Primary tool:
- `coord_team_replace_member team_id="my-team" old_member_id="coder-1" new_member_id="coder-1b"`

Notes:
- Stops old pane and spawns the replacement by default.
