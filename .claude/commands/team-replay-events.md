# /team-replay-events

Replay a team's event/message ledgers into derived summaries and validate event ordering.

Use `coord_team_replay_events` with:
- `team_id`
- optional `write_report`
- optional `force` (bypass parity gate)
