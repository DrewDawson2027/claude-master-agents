# Weekly Ops Digest

Generate a high-level weekly digest (team health + recovery sweep summaries + cost snapshots).

Run:
- `python3 ~/.claude/scripts/weekly_ops_digest.py`

Output:
- `~/.claude/reports/weekly-ops-digest-*.md`
