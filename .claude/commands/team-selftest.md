# /team-selftest

Run runtime/tmux/cost/dashboard/message health checks and write a selftest report.

Primary tool:
- `coord_team_selftest team_id="my-team" cost_timeout=12`

Output:
- report file under `~/.claude/teams/<team>/selftest-*.json`
