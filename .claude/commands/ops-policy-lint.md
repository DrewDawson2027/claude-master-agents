Validate all governance, cost, and team policy configurations.

Steps:
1. Run: `python3 ~/.claude/scripts/policy_engine.py lint`
2. Display pass/fail for each config check
3. Flag any failures that need attention
