# /team-repair-state

Repair malformed team state files/ledgers with backups (dry-run by default).

Use `coord_team_repair_state` with:
- `team_id`
- optional `apply` (otherwise dry-run)
- optional `write_report`
- optional `force` (bypass parity gate)
